# Map View Reference (Placeholder)

**To be added later:** a screenshot of the map view, exported from Claude Design.

- **Screen/component:** The visual map connecting sensitive-data findings to cloud assets — showing where secrets were found, which assets hold the most critical secrets, and asset exposure.
- **Use once added:** Claude Code should use this as the visual source of truth for the map layout, how assets and findings are linked, and how criticality/exposure are visually encoded.
