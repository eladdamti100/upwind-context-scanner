# evenup dataset (real)

Real scanner handoff data. Runtime JSON the app imports lives in `src/data/evenup/` (findings.json, assets.json, manifest.json). `training.csv` is the LightGBM training set and `scanner.db` is the source SQLite DB (provenance, not imported by the UI).
